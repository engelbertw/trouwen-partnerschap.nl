# Database
# PostgreSQL connection string (Neon, Supabase, or local)
# Example: postgresql://user:password@host:5432/database?sslmode=require
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# Clerk Authentication
# Get these from https://dashboard.clerk.com
# Create an application and copy the keys from the API Keys section
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your_publishable_key_here
CLERK_SECRET_KEY=sk_test_your_secret_key_here

# BAG API (Kadaster)
# Register at https://www.kadaster.nl/zakelijk/producten/bag-api
# Optional: Only needed for automatic address validation
BAG_API_KEY=your_bag_api_key_here

# Resend Email (optional)
# Get from https://resend.com
# Only needed if you want to send emails
RESEND_API_KEY=re_your_resend_api_key_here

# Application URL
# Used for generating absolute URLs in emails and redirects
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Node Environment
# development | production | test
NODE_ENV=development

# Optional: Keycloak Configuration (for DigiD integration)
# Only needed if you're using Keycloak for DigiD authentication
# KEYCLOAK_BASE_URL=https://auth.yourdomain.nl
# REALM=nl-huwelijk
# DIGID_IDP_METADATA_URL=https://was-preprod1.digid.nl/saml/idp/metadata
# DIGID_REQUIRED_LOA=midden
